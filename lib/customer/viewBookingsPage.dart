import 'package:flutter/material.dart';

class ViewBookingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("View Bookings"),
      ),
      body: Center(
        child: Text("View Bookings Page"),
      ),
    );
  }
}
