import 'package:flutter/material.dart';

class CancelBookingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Cancel Booking"),
      ),
      body: Center(
        child: Text("Cancel Booking Page"),
      ),
    );
  }
}
