import 'package:flutter/material.dart';

class CreatePayslipPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Create Payslip"),
      ),
      body: Center(
        child: Text("Create Payslip Page"),
      ),
    );
  }
}
