import 'package:flutter/material.dart';

class ApproveFeedbackPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Approve Feedback"),
      ),
      body: Center(
        child: Text("Approve Feedback Page"),
      ),
    );
  }
}
